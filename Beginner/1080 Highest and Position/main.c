#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num[5],i;


    for(i=0; i<5; i++)
    {
        scanf("%d",&num[i]);
    }

    rem[0]=num[0];

    for(i=0; i<5; i++)
    {
        if(rem)
        printf("%d\n",num[i]);
    }

    return 0;
}
